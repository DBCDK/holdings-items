{
    "appName": "${APPLICATION_NAME}",
    "jdbc": {
        "jdbc/holdingsitemsupdate/holdingsitems": {
            "xa": false,
            "url": "${HOLDINGS_ITEMS_POSTGRES_URL}",
            "maxSize": "${MAX_POOL_SIZE}",
            "resizeQuantity": "${POOL_RESIZE_QUANTITY}",
            "steadySize": "${STEADY_POOL_SIZE}"
        }
    },
    "logger": {
        "dk.dbc": {
            "level" : "${LOG__dk_dbc}"
        }
    }
}
