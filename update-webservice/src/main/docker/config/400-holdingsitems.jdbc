[jdbc/holdingsitemsupdate/holdingsitems]
"${HOLDINGS_ITEMS_POSTGRES_URL}"

ApplicationName = "${KAFKA_LOG_APPNAME}-${HOSTNAME}"

max-pool-size = ${MAX_POOL_SIZE|8}
pool-resize-quantity = ${POOL_RESIZE_QUANTITY|2}
steady-pool-size = ${STEADY_POOL_SIZE|2}
