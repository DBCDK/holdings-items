{
    "appName": "HoldingsItemsSolrDocStoreIndexer",
    "jdbc": {
        "jdbc/holdings-items": {
            "url": "${HOLDINGS_ITEMS_POSTGRES_URL}",
            "maxSize": "${MAX_POOL_SIZE}",
            "steadySize":"0"
        }
    },
    "logger": {
        "dk.dbc": {
            "level" : "${LOG__dk_dbc}"
        }
    }
}
